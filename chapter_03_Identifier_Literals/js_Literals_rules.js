// ============================================
// JAVASCRIPT LITERALS - COMPLETE EXAMPLES
// ============================================

// 1. STRING LITERALS
let str1 = 'Single quotes';
let str2 = "Double quotes";
let str3 = `Template literal with ${variable}`;

// 2. NUMERIC LITERALS
let int = 42;
let float = 3.14;
let hex = 0xFF;
let binary = 0b1010;
let scientific = 1.5e2;

// 3. BOOLEAN LITERALS
let isTrue = true;
let isFalse = false;

// 4. NULL & UNDEFINED
let nullVal = null;
let undefinedVal = undefined;

// 5. OBJECT LITERALS
let person = {
    name: "Prachi",
    age: 25,
    greet: function() { return "Hello"; }
};

// 6. ARRAY LITERALS
let arr = [1, 2, 3, "four", { five: 5 }];

// 7. FUNCTION LITERALS
let add = (a, b) => a + b;

// 8. REGEX LITERALS
let pattern = /[0-9]+/gi;

// 9. SYMBOL LITERALS
let sym = Symbol('unique');